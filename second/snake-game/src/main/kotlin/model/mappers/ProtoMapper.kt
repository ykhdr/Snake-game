package model.mappers

import me.ippolitov.fit.snakes.SnakesProto
import model.dto.messages.GameMessage

/**
 * Маппер из обьектов бизнес логики в Proto
 */
object ProtoMapper{
    fun toMessage(protoMessage: SnakesProto.GameMessage) : GameMessage{

    }
}